include "common.conf"

appName = "deversity-frontend"

cjww {
  dev-phase = "alpha"
  dev-phase = ${?DEV_PHASE}
}

play {
  http {
    router = deversity.Routes
    errorHandler = "utils.application.ErrorHandler"
  }
}
